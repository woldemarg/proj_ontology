# proj_ontology — Python 3.10+
# Install: pip install -r requirements.txt

# Core numerics & ML (OMP, cosine similarity, embeddings)
numpy>=1.24,<3
scikit-learn>=1.3,<2

# Wikipedia ingest & chunking
wikipedia>=1.4
langchain-text-splitters>=0.2

# Embeddings (pulls PyTorch on first install)
sentence-transformers>=2.2

# Neo4j publish & verification
neo4j>=5.0,<6

# Topological Manifold — Louvain L1 (SUPER_CONCEPT_OF)
networkx>=3.0

# Visualization (interactive sphere HTML)
prosphera>=1.0.5
plotly>=5.0
seaborn>=0.13
